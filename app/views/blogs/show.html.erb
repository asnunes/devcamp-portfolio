
<h2><%= @blog.title %></h2>

<p>
  <%= link_to 'Edit', edit_blog_path(@blog) if logged_in?(:site_editor) %> |
  <%= link_to 'New', new_blog_path(@blog) if logged_in?(:site_editor) %>
</p>

<p>
  <%= @blog.body %>
</p>

<%= link_to 'Back', blogs_path %>
